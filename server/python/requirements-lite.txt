# PanicSense Python Dependencies - FREE TIER OPTIMIZED
# Lightweight version for Render Free Tier (512 MB RAM)
# Heavy ML packages (torch, transformers) are excluded

langdetect==1.0.9
nltk==3.8.1
pandas==2.1.4
numpy==1.26.4
scikit-learn==1.3.2
tqdm==4.66.2
python-dotenv==1.0.1
protobuf==4.25.3
pytz==2024.1

# Note: torch and transformers excluded for free tier
# These require 1-2 GB RAM which exceeds free tier limits
# The app will use Groq API for AI instead (cloud-based)
