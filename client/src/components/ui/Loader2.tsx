
import { Loader2 as Loader2Icon } from "lucide-react";
import { ComponentProps } from "react";

const Loader2 = (props: ComponentProps<typeof Loader2Icon>) => {
  return <Loader2Icon {...props} />;
};

export default Loader2;
